# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python

# Virtual environment — committed dependencies via requirements.txt instead
venv/
.venv/
env/

# Unity build output — multi-GB, regeneratable from source
footsies-env/Build/
footsies-env/Library/
footsies-env/Temp/
footsies-env/Logs/
footsies-env/obj/
footsies-env/UserSettings/
footsies-env/MemoryCaptures/
*.app/

# Trained models — too large for git, link via Releases if needed
models/
models_no_pbrs/
*.zip
!scenarios.zip

# Training logs and TensorBoard event files
logs/
logs_no_pbrs/
*.tfevents.*
events.out.tfevents.*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Results — kept in repo (small, important for reproducibility)
# !results/

# Sphinx build output — regeneratable with `make html` in docs/
docs/_build/

# Generated screenshots from visualize_agent.py
screenshots/

# Local notes / scratch
scratch/
notes_local.md
snippets.txt

# Claude Code project memory — not part of the codebase
.claude/

# Thesis document drafts — tracked separately if needed
lucrare_scrisa/
